arduino_read.py
	Python program that communicates with arduino through serial and store data as input.txt (time and voltage), output.txt (time and motor velocity).

system_id_Firday, system_id (fs=100hz)
	Examples in reading and analyzing the recorded data (input.txt, output.txt). The data_process.m is the matlab script that reads data and do some clean up such as remove bad data points. This script should not be used directly on other data sets.
